# Django settings for house_wbs project
# Copy this file to .env and customize for your environment

# ============================================================================
# Core Settings
# ============================================================================

# Secret key for cryptographic signing (generate a new one for production)
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=change-me-to-random-secret-key-in-production

# Debug mode (NEVER True in production!)
DEBUG=True

# Allowed hosts for this deployment (comma-separated)
ALLOWED_HOSTS=127.0.0.1,localhost

# CSRF trusted origins for forms (comma-separated)
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# ============================================================================
# Database Configuration
# ============================================================================

# Database URL format: postgresql://user:pass@host:port/dbname
# or sqlite:///path/to/db.sqlite3 (default)
DATABASE_URL=sqlite:///db.sqlite3

# ============================================================================
# Security Settings (Production Only)
# ============================================================================

# Redirect all HTTP to HTTPS (enable in production with HTTPS)
SECURE_SSL_REDIRECT=False

# Set secure flag on session cookies (enable in production with HTTPS)
SESSION_COOKIE_SECURE=False

# Set secure flag on CSRF cookies (enable in production with HTTPS)
CSRF_COOKIE_SECURE=False

# HSTS settings (enable in production for HTTPS-only enforcement)
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# ============================================================================
# Production Deployment Checklist
# ============================================================================
# Before deploying to production:
# 1. Generate new SECRET_KEY and set it in .env
# 2. Set DEBUG=False
# 3. Configure ALLOWED_HOSTS for your domain
# 4. Set up HTTPS and enable SECURE_SSL_REDIRECT=True
# 5. Enable SESSION_COOKIE_SECURE=True and CSRF_COOKIE_SECURE=True
# 6. Configure SECURE_HSTS_* settings for HSTS header (requires HTTPS)
# 7. Set up database (PostgreSQL recommended for production)
# 8. Run: python manage.py collectstatic
# 9. Run: python manage.py migrate
# 10. Create superuser: python manage.py createsuperuser
# 11. Run tests: python manage.py test

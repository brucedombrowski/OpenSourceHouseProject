## FreeCAD Workspace

This directory is reserved for FreeCAD models, macros, and integration artifacts that will later connect to the Django project management app. Keep files organized with clear subfolders (e.g., `models/`, `macros/`, `exports/`) and document any scripts or workflows you add.

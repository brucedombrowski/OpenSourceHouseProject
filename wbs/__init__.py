"""House Project core app (internal name: ``wbs``).

The Django app keeps the short code-oriented name ``wbs`` to avoid breaking
database tables, migrations, and import paths. User-facing branding uses
"House Project" via AppConfig verbose_name and admin labels.
"""
